{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"wishart-process-em","text":"<p>Finite-basis Wishart process models of neural noise covariance, fit by marginal-likelihood (QMC + Laplace) EM.</p> <p><code>wishart-process-em</code> estimates how the trial-to-trial covariance of a neural population changes smoothly across continuously-parameterised experimental conditions (grating orientation, reach angle, tone frequency, \u2026). By pooling statistical power from neighbouring conditions, it produces accurate covariance estimates even in the challenging few-trials-per-condition regime.</p> <p>It is a self-contained JAX reimplementation of the Wishart process model of Nejatbakhsh, Garon &amp; Williams (2023), with two deliberate differences from the reference implementation:</p> <ol> <li>Finite (Fourier-feature) basis. Each latent Gaussian process is    represented in weight space by a truncated Fourier expansion, turning the    nonparametric GP into a finite, differentiable model.</li> <li>Marginal-likelihood / EM-style inference instead of variational    inference. Per-trial latents are integrated out with a randomised    quasi-Monte-Carlo (Korobov) lattice and an optional    Laplace-approximation importance-sampling correction; the marginal    likelihood is then maximised with <code>optax</code>.</li> </ol> <p>It supports Gaussian, Poisson, and negative-binomial observations and the full covariance family from the paper, \\(\\Sigma(x) = L\\,(U(x)U(x)^\\top + \\Lambda(x))\\,L^\\top\\).</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install wishart-process-em            # from PyPI\n# or, from source:\ngit clone https://github.com/neurostatslab/wishart-process-em\ncd wishart-process-em\npip install -e \".[dev]\"\n</code></pre> <p>Requires Python \u2265 3.10. For plotting and baselines, install the extras: <code>pip install \"wishart-process-em[viz]\"</code>.</p>"},{"location":"#quickstart","title":"Quickstart","text":"<pre><code>import jax.numpy as jnp, jax.random as jxr\nfrom wishart_process_em import (\n    TruncatedFourierBasis, WishartProcessModel, fit, squared_exponential,\n)\n\n# A smooth GP basis over a 1-D periodic condition (e.g. orientation).\nbasis = TruncatedFourierBasis(max_freq=8, num_dims=1,\n                              spectral_density=squared_exponential(lengthscale=0.2))\n\n# A Gaussian Wishart process over N=25 neurons, rank P=3.\nmodel = WishartProcessModel(basis, num_neurons=25, rank=3, likelihood=\"gaussian\")\n\n# Simulate some data from the generative model.\ntrue_params = model.init_params(jxr.PRNGKey(0))\nX = jnp.linspace(0, 1, 400)                       # 400 trials, condition in [0,1]\nY, _ = model.sample(jxr.PRNGKey(1), true_params, X)\n\n# Fit and predict smooth condition-dependent covariance.\nresult = fit(model, Y, X, num_steps=500, learning_rate=1e-1)\nSigma = model.predict_cov(result.params, jnp.linspace(0, 1, 50))   # (50, N, N)\n</code></pre> <p>For spike counts, pass <code>likelihood=\"poisson\"</code> (or <code>\"negative_binomial\"</code>); the latent integral is then estimated by QMC automatically.</p>"},{"location":"#documentation","title":"Documentation","text":"<ul> <li>Model \u2014 the full mathematical description: generative model,   weight-space GP basis, covariance family, likelihoods, and the three   marginal-likelihood estimators.</li> <li>Quickstart \u2014 a longer walkthrough: simulate, fit, predict   covariance, plot covariance ellipses in PCA space, and the Poisson variant.</li> <li>Command line \u2014 the <code>wishart-em</code> CLI for fitting, evaluating, and   sampling from the shell.</li> <li>API reference \u2014 every public symbol, documented from the source.</li> </ul>"},{"location":"#citation","title":"Citation","text":"<p>If you use this software, please cite the paper:</p> <pre><code>@inproceedings{nejatbakhsh2023wishart,\n  title     = {Estimating Noise Correlations Across Continuous Conditions With Wishart Processes},\n  author    = {Nejatbakhsh, Amin and Garon, Isabel and Williams, Alex H.},\n  booktitle = {Advances in Neural Information Processing Systems (NeurIPS)},\n  year      = {2023},\n}\n</code></pre>"},{"location":"#license","title":"License","text":"<p>MIT \u2014 see the LICENSE file.</p>"},{"location":"api/","title":"API reference","text":"<p>This page documents every public symbol exported by <code>wishart_process_em</code> (everything in <code>wishart_process_em.__all__</code>), grouped by area. The docstrings are rendered directly from the source with mkdocstrings.</p>"},{"location":"api/#model","title":"Model","text":""},{"location":"api/#wishart_process_em.WishartProcessModel","title":"wishart_process_em.WishartProcessModel","text":"<pre><code>WishartProcessModel(basis: TruncatedFourierBasis, num_neurons: int, rank: int = 2, likelihood: str | Likelihood = 'gaussian', use_diagonal: bool = False, use_scale: bool = False, jitter: float = 1e-05, init_weight_scale: float = 1.0, prior_weight_scale: float = 1.0)\n</code></pre> <p>Finite-basis Wishart process over smoothly-parameterised conditions.</p> <p>Parameters:</p> Name Type Description Default <code>basis</code> <code>TruncatedFourierBasis</code> <p>Weight-space GP basis shared by the mean and covariance functions.</p> required <code>num_neurons</code> <code>int</code> <p>Number of neurons <code>N</code>.</p> required <code>rank</code> <code>int</code> <p>Rank <code>P</code> of the low-rank covariance factor <code>U</code>.  May be <code>0</code> if <code>use_diagonal=True</code> (a purely diagonal / heteroscedastic model).</p> <code>2</code> <code>likelihood</code> <code>str or Likelihood</code> <p>Observation model: <code>\"gaussian\"</code>, <code>\"poisson\"</code> or <code>\"negative_binomial\"</code> (or a :class:<code>Likelihood</code> instance).</p> <code>'gaussian'</code> <code>use_diagonal</code> <code>bool</code> <p>Include the non-negative diagonal term :math:<code>\\Lambda(x)</code> (the <code>WPlrd</code> extension).  This raises the latent dimension to <code>P + N</code> for count models, so keep it off (default) when relying on low-dimensional QMC.</p> <code>False</code> <code>use_scale</code> <code>bool</code> <p>Include the learnable lower-triangular scale matrix <code>L</code> (initialise it from the grand empirical covariance via :meth:<code>init_params</code>).</p> <code>False</code> <code>jitter</code> <code>float</code> <p>Small value added to covariance diagonals for numerical stability.</p> <code>1e-05</code> <code>init_weight_scale</code> <code>float</code> <p>Standard deviation for random initialisation of the GP weights.</p> <code>1.0</code> <code>prior_weight_scale</code> <code>float</code> <p>Standard deviation of the (Gaussian) prior on the GP weights; <code>1.0</code> corresponds to the standard weight-space GP prior.</p> <code>1.0</code> Source code in <code>src/wishart_process_em/model.py</code> <pre><code>def __init__(\n    self,\n    basis: TruncatedFourierBasis,\n    num_neurons: int,\n    rank: int = 2,\n    likelihood: str | Likelihood = \"gaussian\",\n    use_diagonal: bool = False,\n    use_scale: bool = False,\n    jitter: float = 1e-5,\n    init_weight_scale: float = 1.0,\n    prior_weight_scale: float = 1.0,\n) -&gt; None:\n    if rank &lt; 0:\n        raise ValueError(\"rank P must be &gt;= 0\")\n    if rank == 0 and not use_diagonal:\n        raise ValueError(\"rank=0 requires use_diagonal=True (else Sigma is zero)\")\n\n    self.basis = basis\n    self.num_neurons = int(num_neurons)\n    self.rank = int(rank)\n    self.likelihood: Likelihood = get_likelihood(likelihood)\n    self.use_diagonal = bool(use_diagonal)\n    self.use_scale = bool(use_scale)\n    self.jitter = float(jitter)\n    self.init_weight_scale = float(init_weight_scale)\n    self.prior_weight_scale = float(prior_weight_scale)\n</code></pre>"},{"location":"api/#wishart_process_em.WishartProcessModel.latent_dim","title":"latent_dim  <code>property</code>","text":"<pre><code>latent_dim: int\n</code></pre> <p>Dimension <code>Q</code> of the per-trial latent <code>v</code> to be integrated.</p>"},{"location":"api/#wishart_process_em.WishartProcessModel.init_params","title":"init_params","text":"<pre><code>init_params(key: Array, mean_bias: ndarray | None = None, scale_tril: ndarray | None = None) -&gt; WPParams\n</code></pre> <p>Randomly initialise model parameters.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Array</code> <p>PRNG key.</p> required <code>mean_bias</code> <code>ndarray</code> <p>Per-neuron mean bias <code>(N,)</code> (e.g. inverse-link of the empirical mean response).  Defaults to zeros.</p> <code>None</code> <code>scale_tril</code> <code>ndarray</code> <p>Lower-triangular initialiser for the scale matrix <code>L</code> (e.g. the Cholesky factor of the grand empirical covariance).  Only used when <code>use_scale=True</code>; defaults to the identity.</p> <code>None</code> Source code in <code>src/wishart_process_em/model.py</code> <pre><code>def init_params(\n    self,\n    key: jax.Array,\n    mean_bias: jnp.ndarray | None = None,\n    scale_tril: jnp.ndarray | None = None,\n) -&gt; WPParams:\n    \"\"\"Randomly initialise model parameters.\n\n    Parameters\n    ----------\n    key : jax.Array\n        PRNG key.\n    mean_bias : jnp.ndarray, optional\n        Per-neuron mean bias ``(N,)`` (e.g. inverse-link of the empirical\n        mean response).  Defaults to zeros.\n    scale_tril : jnp.ndarray, optional\n        Lower-triangular initialiser for the scale matrix ``L`` (e.g. the\n        Cholesky factor of the grand empirical covariance).  Only used when\n        ``use_scale=True``; defaults to the identity.\n    \"\"\"\n    n, p, m = self.num_neurons, self.rank, self.basis.num_features\n    k_mean, k_fac, k_diag, k_lik = jxr.split(key, 4)\n    s = self.init_weight_scale\n\n    mean_w = s * jxr.normal(k_mean, (n, m))\n    mean_b = jnp.zeros(n) if mean_bias is None else jnp.asarray(mean_bias)\n    factor_w = s * jxr.normal(k_fac, (n, p, m))\n    log_latent_scale = jnp.full((p,), softplus_inverse(jnp.asarray(1.0)))\n\n    diag_w = s * jxr.normal(k_diag, (n, m)) if self.use_diagonal else None\n\n    scale_raw = None\n    if self.use_scale:\n        from .covariance import raw_from_scale_tril\n\n        init_tril = jnp.eye(n) if scale_tril is None else jnp.asarray(scale_tril)\n        scale_raw = raw_from_scale_tril(init_tril)\n\n    lik = self.likelihood.init_params(n, k_lik)\n    return WPParams(mean_w, mean_b, factor_w, log_latent_scale, diag_w, scale_raw, lik)\n</code></pre>"},{"location":"api/#wishart_process_em.WishartProcessModel.mean_at","title":"mean_at","text":"<pre><code>mean_at(params: WPParams, x: ndarray) -&gt; jnp.ndarray\n</code></pre> <p>Latent mean predictor <code>mu(x)</code>, shape <code>(N,)</code>.</p> Source code in <code>src/wishart_process_em/model.py</code> <pre><code>def mean_at(self, params: WPParams, x: jnp.ndarray) -&gt; jnp.ndarray:\n    \"\"\"Latent mean predictor ``mu(x)``, shape ``(N,)``.\"\"\"\n    phi = self.basis.features(x)\n    return params.mean_w @ phi + params.mean_b\n</code></pre>"},{"location":"api/#wishart_process_em.WishartProcessModel.factor_at","title":"factor_at","text":"<pre><code>factor_at(params: WPParams, x: ndarray) -&gt; jnp.ndarray\n</code></pre> <p>Low-rank covariance factor <code>U(x) = A(x) diag(sZ)</code>, shape <code>(N, P)</code>.</p> Source code in <code>src/wishart_process_em/model.py</code> <pre><code>def factor_at(self, params: WPParams, x: jnp.ndarray) -&gt; jnp.ndarray:\n    \"\"\"Low-rank covariance factor ``U(x) = A(x) diag(sZ)``, shape ``(N, P)``.\"\"\"\n    phi = self.basis.features(x)\n    a = jnp.einsum(\"npm,m-&gt;np\", params.factor_w, phi)  # (N, P)\n    sz = jax.nn.softplus(params.log_latent_scale)  # (P,)\n    return a * sz[None, :]\n</code></pre>"},{"location":"api/#wishart_process_em.WishartProcessModel.diag_at","title":"diag_at","text":"<pre><code>diag_at(params: WPParams, x: ndarray) -&gt; jnp.ndarray\n</code></pre> <p>Non-negative diagonal <code>lambda(x)</code>, shape <code>(N,)</code> (zeros if disabled).</p> Source code in <code>src/wishart_process_em/model.py</code> <pre><code>def diag_at(self, params: WPParams, x: jnp.ndarray) -&gt; jnp.ndarray:\n    \"\"\"Non-negative diagonal ``lambda(x)``, shape ``(N,)`` (zeros if disabled).\"\"\"\n    if not self.use_diagonal:\n        return jnp.zeros(self.num_neurons)\n    phi = self.basis.features(x)\n    return jax.nn.softplus(params.diag_w @ phi)\n</code></pre>"},{"location":"api/#wishart_process_em.WishartProcessModel.scale_tril","title":"scale_tril","text":"<pre><code>scale_tril(params: WPParams) -&gt; jnp.ndarray\n</code></pre> <p>Scale matrix <code>L</code> (identity if <code>use_scale=False</code>), shape <code>(N, N)</code>.</p> Source code in <code>src/wishart_process_em/model.py</code> <pre><code>def scale_tril(self, params: WPParams) -&gt; jnp.ndarray:\n    \"\"\"Scale matrix ``L`` (identity if ``use_scale=False``), shape ``(N, N)``.\"\"\"\n    if not self.use_scale:\n        return jnp.eye(self.num_neurons)\n    return scale_tril_from_raw(params.scale_raw)\n</code></pre>"},{"location":"api/#wishart_process_em.WishartProcessModel.sqrt_factor_at","title":"sqrt_factor_at","text":"<pre><code>sqrt_factor_at(params: WPParams, x: ndarray) -&gt; jnp.ndarray\n</code></pre> <p>Covariance square root <code>G(x)</code> with <code>G G^T = Sigma(x)</code>.</p> <p>Shape <code>(N, Q)</code> where <code>Q = P</code> (or <code>P + N</code> with the diagonal term). The latent predictor is <code>eta = mu(x) + G(x) v</code>, <code>v ~ N(0, I_Q)</code>.</p> Source code in <code>src/wishart_process_em/model.py</code> <pre><code>def sqrt_factor_at(self, params: WPParams, x: jnp.ndarray) -&gt; jnp.ndarray:\n    r\"\"\"Covariance square root ``G(x)`` with ``G G^T = Sigma(x)``.\n\n    Shape ``(N, Q)`` where ``Q = P`` (or ``P + N`` with the diagonal term).\n    The latent predictor is ``eta = mu(x) + G(x) v``, ``v ~ N(0, I_Q)``.\n    \"\"\"\n    u = self.factor_at(params, x)  # (N, P)\n    L = self.scale_tril(params)\n    if self.use_diagonal:\n        d = jnp.diag(jnp.sqrt(self.diag_at(params, x)))  # (N, N)\n        b = jnp.concatenate([u, d], axis=1)  # (N, P + N)\n    else:\n        b = u\n    return L @ b\n</code></pre>"},{"location":"api/#wishart_process_em.WishartProcessModel.cov_at","title":"cov_at","text":"<pre><code>cov_at(params: WPParams, x: ndarray) -&gt; jnp.ndarray\n</code></pre> <p>Latent covariance <code>Sigma(x) = G G^T + jitter I</code>, shape <code>(N, N)</code>.</p> Source code in <code>src/wishart_process_em/model.py</code> <pre><code>def cov_at(self, params: WPParams, x: jnp.ndarray) -&gt; jnp.ndarray:\n    \"\"\"Latent covariance ``Sigma(x) = G G^T + jitter I``, shape ``(N, N)``.\"\"\"\n    g = self.sqrt_factor_at(params, x)\n    return g @ g.T + self.jitter * jnp.eye(self.num_neurons)\n</code></pre>"},{"location":"api/#wishart_process_em.WishartProcessModel.predict_mean_latent","title":"predict_mean_latent","text":"<pre><code>predict_mean_latent(params: WPParams, X: ndarray) -&gt; jnp.ndarray\n</code></pre> <p>Latent mean <code>mu(x)</code> for each condition, shape <code>(T, N)</code>.</p> Source code in <code>src/wishart_process_em/model.py</code> <pre><code>def predict_mean_latent(self, params: WPParams, X: jnp.ndarray) -&gt; jnp.ndarray:\n    \"\"\"Latent mean ``mu(x)`` for each condition, shape ``(T, N)``.\"\"\"\n    return jax.vmap(lambda x: self.mean_at(params, x))(_as_conditions(X))\n</code></pre>"},{"location":"api/#wishart_process_em.WishartProcessModel.predict_mean","title":"predict_mean","text":"<pre><code>predict_mean(params: WPParams, X: ndarray) -&gt; jnp.ndarray\n</code></pre> <p>Expected observation <code>E[y|x] = link(mu(x))</code>, shape <code>(T, N)</code>.</p> Source code in <code>src/wishart_process_em/model.py</code> <pre><code>def predict_mean(self, params: WPParams, X: jnp.ndarray) -&gt; jnp.ndarray:\n    \"\"\"Expected observation ``E[y|x] = link(mu(x))``, shape ``(T, N)``.\"\"\"\n    mu = self.predict_mean_latent(params, X)\n    return jax.vmap(lambda e: self.likelihood.mean(params.lik, e))(mu)\n</code></pre>"},{"location":"api/#wishart_process_em.WishartProcessModel.predict_cov","title":"predict_cov","text":"<pre><code>predict_cov(params: WPParams, X: ndarray) -&gt; jnp.ndarray\n</code></pre> <p>Latent covariance <code>Sigma(x)</code> for each condition, shape <code>(T, N, N)</code>.</p> <p>For the Gaussian likelihood this is exactly the covariance of <code>y</code>.  For count likelihoods it is the covariance of the linear predictor; use :meth:<code>predict_observed_cov</code> for the covariance of the counts themselves.</p> Source code in <code>src/wishart_process_em/model.py</code> <pre><code>def predict_cov(self, params: WPParams, X: jnp.ndarray) -&gt; jnp.ndarray:\n    \"\"\"Latent covariance ``Sigma(x)`` for each condition, shape ``(T, N, N)``.\n\n    For the Gaussian likelihood this is exactly the covariance of ``y``.  For\n    count likelihoods it is the covariance of the *linear predictor*; use\n    :meth:`predict_observed_cov` for the covariance of the counts themselves.\n    \"\"\"\n    return jax.vmap(lambda x: self.cov_at(params, x))(_as_conditions(X))\n</code></pre>"},{"location":"api/#wishart_process_em.WishartProcessModel.predict_observed_cov","title":"predict_observed_cov","text":"<pre><code>predict_observed_cov(key: Array, params: WPParams, X: ndarray, num_samples: int = 500) -&gt; jnp.ndarray\n</code></pre> <p>Monte-Carlo covariance of the observations <code>y</code> at each condition.</p> <p>Necessary for non-Gaussian likelihoods, where the observation covariance differs from <code>Sigma</code> because of the link and the count noise.</p> Source code in <code>src/wishart_process_em/model.py</code> <pre><code>def predict_observed_cov(\n    self, key: jax.Array, params: WPParams, X: jnp.ndarray, num_samples: int = 500\n) -&gt; jnp.ndarray:\n    \"\"\"Monte-Carlo covariance of the *observations* ``y`` at each condition.\n\n    Necessary for non-Gaussian likelihoods, where the observation covariance\n    differs from ``Sigma`` because of the link and the count noise.\n    \"\"\"\n    X = _as_conditions(X)\n    keys = jxr.split(key, num_samples)\n    # (num_samples, T, N)\n    samples = jax.vmap(lambda k: self.sample(k, params, X)[0])(keys)\n    # covariance across samples, per condition.\n    return jax.vmap(lambda s: jnp.cov(s, rowvar=False), in_axes=1)(samples)\n</code></pre>"},{"location":"api/#wishart_process_em.WishartProcessModel.sample","title":"sample","text":"<pre><code>sample(key: Array, params: WPParams, X: ndarray) -&gt; tuple[jnp.ndarray, jnp.ndarray]\n</code></pre> <p>Sample observations for a batch of conditions.</p> <p>Returns <code>(y, eta)</code> with shapes <code>(T, N)</code> each, where <code>eta</code> is the latent linear predictor.</p> Source code in <code>src/wishart_process_em/model.py</code> <pre><code>def sample(\n    self, key: jax.Array, params: WPParams, X: jnp.ndarray\n) -&gt; tuple[jnp.ndarray, jnp.ndarray]:\n    \"\"\"Sample observations for a batch of conditions.\n\n    Returns ``(y, eta)`` with shapes ``(T, N)`` each, where ``eta`` is the\n    latent linear predictor.\n    \"\"\"\n    X = _as_conditions(X)\n    t = X.shape[0]\n    k_lat, k_obs = jxr.split(key)\n    v = jxr.normal(k_lat, (t, self.latent_dim))\n\n    def one(x, vi, ko):\n        eta = self.mean_at(params, x) + self.sqrt_factor_at(params, x) @ vi\n        y = self.likelihood.observe(ko, params.lik, eta)\n        return y, eta\n\n    obs_keys = jxr.split(k_obs, t)\n    return jax.vmap(one)(X, v, obs_keys)\n</code></pre>"},{"location":"api/#wishart_process_em.WishartProcessModel.log_prior","title":"log_prior","text":"<pre><code>log_prior(params: WPParams) -&gt; jnp.ndarray\n</code></pre> <p>Log-prior over parameters (the weight-space GP prior + likelihood).</p> <p>A standard-normal prior on the GP weights corresponds to the GP prior on the mean and covariance-factor functions.</p> Source code in <code>src/wishart_process_em/model.py</code> <pre><code>def log_prior(self, params: WPParams) -&gt; jnp.ndarray:\n    r\"\"\"Log-prior over parameters (the weight-space GP prior + likelihood).\n\n    A standard-normal prior on the GP weights corresponds to the GP prior on\n    the mean and covariance-factor functions.\n    \"\"\"\n    scale = self.prior_weight_scale\n\n    def normal_lp(w):\n        return jnp.sum(jax.scipy.stats.norm.logpdf(w, 0.0, scale))\n\n    lp = normal_lp(params.mean_w) + normal_lp(params.factor_w)\n    if self.use_diagonal:\n        lp = lp + normal_lp(params.diag_w)\n    lp = lp + self.likelihood.log_prior(params.lik)\n    return lp\n</code></pre>"},{"location":"api/#wishart_process_em.WishartProcessModel.marginal_loglike","title":"marginal_loglike","text":"<pre><code>marginal_loglike(params: WPParams, Y: ndarray, X: ndarray, key: Array | None = None, lattice=None, method: str = 'auto') -&gt; jnp.ndarray\n</code></pre> <p>Sum of per-trial marginal log-likelihoods over a dataset.</p> <p>Thin wrapper around :func:<code>wishart_process_em.inference.dataset_marginal_loglike</code>.</p> Source code in <code>src/wishart_process_em/model.py</code> <pre><code>def marginal_loglike(\n    self,\n    params: WPParams,\n    Y: jnp.ndarray,\n    X: jnp.ndarray,\n    key: jax.Array | None = None,\n    lattice=None,\n    method: str = \"auto\",\n) -&gt; jnp.ndarray:\n    \"\"\"Sum of per-trial marginal log-likelihoods over a dataset.\n\n    Thin wrapper around :func:`wishart_process_em.inference.dataset_marginal_loglike`.\n    \"\"\"\n    from .inference import dataset_marginal_loglike\n\n    return dataset_marginal_loglike(\n        self, params, Y, X, key=key, lattice=lattice, method=method\n    )\n</code></pre>"},{"location":"api/#wishart_process_em.WPParams","title":"wishart_process_em.WPParams","text":"<p>               Bases: <code>NamedTuple</code></p> <p>Trainable parameters of a :class:<code>~wishart_process_em.model.WishartProcessModel</code>.</p> <p>Optional components (<code>diag_w</code>, <code>scale_raw</code>, <code>lik</code>) are <code>None</code> when the corresponding feature is disabled; JAX treats <code>None</code> as an empty subtree, so those components simply receive no gradients.</p>"},{"location":"api/#basis-and-kernels","title":"Basis and kernels","text":""},{"location":"api/#wishart_process_em.TruncatedFourierBasis","title":"wishart_process_em.TruncatedFourierBasis","text":"<pre><code>TruncatedFourierBasis(max_freq: int, num_dims: int, spectral_density: Callable[[ndarray], ndarray], tol: float = 1e-05)\n</code></pre> <p>Truncated Fourier-feature basis for scalar Gaussian processes.</p> <p>A function drawn from this basis has the form</p> <p>.. math::</p> <pre><code>f(x) = \\sum_k \\tau_k \\big( w^{\\sin}_k \\sin(2\\pi k \\cdot x)\n                         + w^{\\cos}_k \\cos(2\\pi k \\cdot x) \\big),\n</code></pre> <p>where the integer frequency vectors :math:<code>k</code> range over <code>{0, ..., max_freq - 1} ** num_dims</code> (excluding the constant mode) and :math:<code>\\tau_k = \\sqrt{S(2\\pi k)}</code> with :math:<code>S</code> the kernel spectral density.  Low-amplitude features (<code>tau</code> below <code>tol</code> times the largest) are pruned for efficiency.</p> <p>Parameters:</p> Name Type Description Default <code>max_freq</code> <code>int</code> <p>Number of integer frequencies enumerated per input dimension.</p> required <code>num_dims</code> <code>int</code> <p>Dimensionality of the input / condition space <code>x</code>.</p> required <code>spectral_density</code> <code>Callable</code> <p>Maps squared angular frequency <code>|omega|**2</code> (shape <code>(n_freq,)</code>) to the spectral density value.  See :mod:<code>wishart_process_em.kernels</code> for constructors (e.g. squared-exponential).  Larger values at high frequency yield rougher functions.</p> required <code>tol</code> <code>float</code> <p>Relative threshold below which features are discarded.</p> <code>1e-05</code> <p>Attributes:</p> Name Type Description <code>frequencies</code> <code>ndarray</code> <p>Angular frequency vectors, shape <code>(n_freq, num_dims)</code>.</p> <code>tau</code> <code>ndarray</code> <p>Per-frequency amplitude <code>sqrt(spectral_density)</code>, shape <code>(n_freq,)</code>.</p> <code>num_features</code> <code>int</code> <p>Length <code>M</code> of the feature vector returned by :meth:<code>features</code> (equal to <code>2 * n_freq</code>).</p> Source code in <code>src/wishart_process_em/basis.py</code> <pre><code>def __init__(\n    self,\n    max_freq: int,\n    num_dims: int,\n    spectral_density: Callable[[jnp.ndarray], jnp.ndarray],\n    tol: float = 1e-5,\n) -&gt; None:\n    if max_freq &lt; 1:\n        raise ValueError(\"max_freq must be &gt;= 1\")\n    if num_dims &lt; 1:\n        raise ValueError(\"num_dims must be &gt;= 1\")\n\n    # Enumerate integer frequency vectors on the grid, dropping the\n    # all-zero (constant) mode which is handled by an explicit bias term\n    # in the model.\n    grid = np.stack(\n        np.unravel_index(\n            np.arange(max_freq**num_dims),\n            shape=tuple(max_freq for _ in range(num_dims)),\n        ),\n        axis=1,\n    )[1:]\n    omega = 2.0 * np.pi * grid  # angular frequencies\n\n    sq_norm = jnp.asarray(np.sum(omega**2, axis=1))\n    tau = jnp.sqrt(jnp.asarray(spectral_density(sq_norm)))\n\n    # Prune low-amplitude features.\n    thresh = jnp.max(tau) * tol\n    keep = np.asarray(tau &gt; thresh)\n\n    self.frequencies = jnp.asarray(omega[keep])\n    self.tau = tau[keep]\n    self.num_features = 2 * int(self.frequencies.shape[0])\n\n    # Bookkeeping / config.\n    self.max_freq = max_freq\n    self.num_dims = num_dims\n    self.spectral_density = spectral_density\n    self.tol = tol\n</code></pre>"},{"location":"api/#wishart_process_em.TruncatedFourierBasis.features","title":"features","text":"<pre><code>features(x: ndarray) -&gt; jnp.ndarray\n</code></pre> <p>Evaluate the (fixed) feature vector at a single input <code>x</code>.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ndarray</code> <p>A single input of shape <code>(num_dims,)</code> (or a scalar when <code>num_dims == 1</code>).</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>Feature vector of shape <code>(num_features,)</code>.</p> Source code in <code>src/wishart_process_em/basis.py</code> <pre><code>def features(self, x: jnp.ndarray) -&gt; jnp.ndarray:\n    \"\"\"Evaluate the (fixed) feature vector at a single input ``x``.\n\n    Parameters\n    ----------\n    x : jnp.ndarray\n        A single input of shape ``(num_dims,)`` (or a scalar when\n        ``num_dims == 1``).\n\n    Returns\n    -------\n    jnp.ndarray\n        Feature vector of shape ``(num_features,)``.\n    \"\"\"\n    x = jnp.atleast_1d(x)\n    u = self.frequencies @ x  # (n_freq,)\n    return jnp.concatenate([self.tau * jnp.sin(u), self.tau * jnp.cos(u)])\n</code></pre>"},{"location":"api/#wishart_process_em.TruncatedFourierBasis.evaluate","title":"evaluate","text":"<pre><code>evaluate(weights: ndarray, x: ndarray) -&gt; jnp.ndarray\n</code></pre> <p>Evaluate <code>sum_m weights_m * features_m(x)</code> for a single <code>x</code>.</p> <p><code>weights</code> has shape <code>(num_features,)</code>.  Under the standard-normal prior on <code>weights</code>, <code>evaluate</code> is a draw from the approximate GP.</p> Source code in <code>src/wishart_process_em/basis.py</code> <pre><code>def evaluate(self, weights: jnp.ndarray, x: jnp.ndarray) -&gt; jnp.ndarray:\n    r\"\"\"Evaluate ``sum_m weights_m * features_m(x)`` for a single ``x``.\n\n    ``weights`` has shape ``(num_features,)``.  Under the standard-normal\n    prior on ``weights``, ``evaluate`` is a draw from the approximate GP.\n    \"\"\"\n    return weights @ self.features(x)\n</code></pre>"},{"location":"api/#wishart_process_em.squared_exponential","title":"wishart_process_em.squared_exponential","text":"<pre><code>squared_exponential(lengthscale: float, variance: float = 1.0) -&gt; SpectralDensity\n</code></pre> <p>Spectral density of a squared-exponential (RBF) kernel.</p> <p>For :math:<code>k(r) = \\text{variance} \\cdot \\exp(-r^2 / (2\\ell^2))</code> the spectral density is proportional to :math:<code>\\exp(-\\ell^2 |\\omega|^2 / 2)</code>.  Smaller <code>lengthscale</code> admits higher frequencies and yields rougher sample paths; this is the analogue of the bandwidth hyperparameter :math:<code>\\lambda</code> in the Wishart-process paper.</p> <p>Parameters:</p> Name Type Description Default <code>lengthscale</code> <code>float</code> <p>Length scale :math:<code>\\ell &gt; 0</code> controlling smoothness.</p> required <code>variance</code> <code>float</code> <p>Marginal variance (overall amplitude) of the process.</p> <code>1.0</code> Source code in <code>src/wishart_process_em/kernels.py</code> <pre><code>def squared_exponential(lengthscale: float, variance: float = 1.0) -&gt; SpectralDensity:\n    r\"\"\"Spectral density of a squared-exponential (RBF) kernel.\n\n    For :math:`k(r) = \\text{variance} \\cdot \\exp(-r^2 / (2\\ell^2))` the spectral\n    density is proportional to :math:`\\exp(-\\ell^2 |\\omega|^2 / 2)`.  Smaller\n    ``lengthscale`` admits higher frequencies and yields rougher sample paths;\n    this is the analogue of the bandwidth hyperparameter :math:`\\lambda` in the\n    Wishart-process paper.\n\n    Parameters\n    ----------\n    lengthscale : float\n        Length scale :math:`\\ell &gt; 0` controlling smoothness.\n    variance : float, optional\n        Marginal variance (overall amplitude) of the process.\n    \"\"\"\n    if lengthscale &lt;= 0:\n        raise ValueError(\"lengthscale must be positive\")\n    ell2 = lengthscale**2\n\n    def density(sq_omega: jnp.ndarray) -&gt; jnp.ndarray:\n        return variance * jnp.exp(-0.5 * ell2 * sq_omega)\n\n    return density\n</code></pre>"},{"location":"api/#wishart_process_em.matern","title":"wishart_process_em.matern","text":"<pre><code>matern(lengthscale: float, nu: float = 1.5, variance: float = 1.0) -&gt; SpectralDensity\n</code></pre> <p>Spectral density of a Matern kernel with smoothness <code>nu</code>.</p> <p>Uses the (unnormalised) isotropic Matern spectral density :math:<code>S(\\omega) \\propto (2\\nu/\\ell^2 + |\\omega|^2)^{-(\\nu + d/2)}</code>.  The dimensional term <code>d/2</code> is absorbed into an effective exponent; here we use the common 1-D form and let <code>variance</code> set the amplitude, which is adequate for a smoothness prior in weight space.</p> <p>Parameters:</p> Name Type Description Default <code>lengthscale</code> <code>float</code> <p>Length scale :math:<code>\\ell &gt; 0</code>.</p> required <code>nu</code> <code>float</code> <p>Smoothness. <code>0.5</code> is exponential (Ornstein-Uhlenbeck); larger is smoother; <code>nu -&gt; inf</code> recovers the squared exponential.</p> <code>1.5</code> <code>variance</code> <code>float</code> <p>Overall amplitude.</p> <code>1.0</code> Source code in <code>src/wishart_process_em/kernels.py</code> <pre><code>def matern(lengthscale: float, nu: float = 1.5, variance: float = 1.0) -&gt; SpectralDensity:\n    r\"\"\"Spectral density of a Matern kernel with smoothness ``nu``.\n\n    Uses the (unnormalised) isotropic Matern spectral density\n    :math:`S(\\omega) \\propto (2\\nu/\\ell^2 + |\\omega|^2)^{-(\\nu + d/2)}`.  The\n    dimensional term ``d/2`` is absorbed into an effective exponent; here we use\n    the common 1-D form and let ``variance`` set the amplitude, which is\n    adequate for a smoothness prior in weight space.\n\n    Parameters\n    ----------\n    lengthscale : float\n        Length scale :math:`\\ell &gt; 0`.\n    nu : float, optional\n        Smoothness. ``0.5`` is exponential (Ornstein-Uhlenbeck); larger is\n        smoother; ``nu -&gt; inf`` recovers the squared exponential.\n    variance : float, optional\n        Overall amplitude.\n    \"\"\"\n    if lengthscale &lt;= 0:\n        raise ValueError(\"lengthscale must be positive\")\n    if nu &lt;= 0:\n        raise ValueError(\"nu must be positive\")\n    scale = 2.0 * nu / lengthscale**2\n    exponent = nu + 0.5\n\n    def density(sq_omega: jnp.ndarray) -&gt; jnp.ndarray:\n        return variance * (scale + sq_omega) ** (-exponent)\n\n    return density\n</code></pre>"},{"location":"api/#likelihoods","title":"Likelihoods","text":""},{"location":"api/#wishart_process_em.Likelihood","title":"wishart_process_em.Likelihood","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract observation model mapping a latent predictor <code>eta</code> to <code>y</code>.</p> <p>Subclasses are frozen dataclasses so they can be used as static arguments to <code>jax.jit</code>.  Trainable per-model parameters are threaded through the <code>lik_params</code> argument (a pytree, possibly <code>None</code>).</p>"},{"location":"api/#wishart_process_em.Likelihood.init_params","title":"init_params","text":"<pre><code>init_params(num_neurons: int, key: Array)\n</code></pre> <p>Return initial trainable likelihood parameters (default: <code>None</code>).</p> Source code in <code>src/wishart_process_em/likelihoods.py</code> <pre><code>def init_params(self, num_neurons: int, key: jax.Array):\n    \"\"\"Return initial trainable likelihood parameters (default: ``None``).\"\"\"\n    return None\n</code></pre>"},{"location":"api/#wishart_process_em.Likelihood.log_prior","title":"log_prior","text":"<pre><code>log_prior(lik_params) -&gt; jnp.ndarray\n</code></pre> <p>Log-prior over the trainable likelihood parameters.</p> Source code in <code>src/wishart_process_em/likelihoods.py</code> <pre><code>def log_prior(self, lik_params) -&gt; jnp.ndarray:\n    \"\"\"Log-prior over the trainable likelihood parameters.\"\"\"\n    return jnp.asarray(0.0)\n</code></pre>"},{"location":"api/#wishart_process_em.Likelihood.log_prob","title":"log_prob  <code>abstractmethod</code>","text":"<pre><code>log_prob(lik_params, y: ndarray, eta: ndarray) -&gt; jnp.ndarray\n</code></pre> <p>Log <code>p(y | eta)</code> for a single trial, summed over neurons.</p> Source code in <code>src/wishart_process_em/likelihoods.py</code> <pre><code>@abc.abstractmethod\ndef log_prob(self, lik_params, y: jnp.ndarray, eta: jnp.ndarray) -&gt; jnp.ndarray:\n    \"\"\"Log ``p(y | eta)`` for a single trial, summed over neurons.\"\"\"\n</code></pre>"},{"location":"api/#wishart_process_em.Likelihood.observe","title":"observe  <code>abstractmethod</code>","text":"<pre><code>observe(key: Array, lik_params, eta: ndarray) -&gt; jnp.ndarray\n</code></pre> <p>Sample observations <code>y</code> given the latent predictor <code>eta</code>.</p> Source code in <code>src/wishart_process_em/likelihoods.py</code> <pre><code>@abc.abstractmethod\ndef observe(self, key: jax.Array, lik_params, eta: jnp.ndarray) -&gt; jnp.ndarray:\n    \"\"\"Sample observations ``y`` given the latent predictor ``eta``.\"\"\"\n</code></pre>"},{"location":"api/#wishart_process_em.Likelihood.mean","title":"mean  <code>abstractmethod</code>","text":"<pre><code>mean(lik_params, eta: ndarray) -&gt; jnp.ndarray\n</code></pre> <p>Expected observation <code>E[y | eta]</code>.</p> Source code in <code>src/wishart_process_em/likelihoods.py</code> <pre><code>@abc.abstractmethod\ndef mean(self, lik_params, eta: jnp.ndarray) -&gt; jnp.ndarray:\n    \"\"\"Expected observation ``E[y | eta]``.\"\"\"\n</code></pre>"},{"location":"api/#wishart_process_em.Gaussian","title":"wishart_process_em.Gaussian  <code>dataclass</code>","text":"<pre><code>Gaussian(learn_obs_noise: bool = False, conjugate_gaussian: bool = True)\n</code></pre> <p>               Bases: <code>Likelihood</code></p> <p>Multivariate-Gaussian observations <code>y ~ N(mu(x), Sigma(x))</code>.</p> <p>Conjugate: fitting uses the exact marginal likelihood via :meth:<code>marginal_log_prob</code>; no latent sampling is required.  An optional homoscedastic observation-noise variance can be added on top of :math:<code>\\Sigma(x)</code> by enabling <code>learn_obs_noise</code>.</p> <p>Parameters:</p> Name Type Description Default <code>learn_obs_noise</code> <code>bool</code> <p>If <code>True</code>, a scalar observation-noise variance is learned and added to the diagonal of the covariance.</p> <code>False</code>"},{"location":"api/#wishart_process_em.Gaussian.marginal_log_prob","title":"marginal_log_prob","text":"<pre><code>marginal_log_prob(lik_params, y, mean, cov) -&gt; jnp.ndarray\n</code></pre> <p>Exact log <code>N(y; mean, cov + obs_var * I)</code> for one trial.</p> Source code in <code>src/wishart_process_em/likelihoods.py</code> <pre><code>def marginal_log_prob(self, lik_params, y, mean, cov) -&gt; jnp.ndarray:\n    \"\"\"Exact log ``N(y; mean, cov + obs_var * I)`` for one trial.\"\"\"\n    n = mean.shape[0]\n    cov = cov + self.obs_var(lik_params) * jnp.eye(n)\n    return jax.scipy.stats.multivariate_normal.logpdf(y, mean, cov)\n</code></pre>"},{"location":"api/#wishart_process_em.Poisson","title":"wishart_process_em.Poisson  <code>dataclass</code>","text":"<pre><code>Poisson()\n</code></pre> <p>               Bases: <code>Likelihood</code></p> <p>Poisson-count observations <code>y_n ~ Poisson(softplus(eta_n))</code>.</p> <p>The softplus inverse-link keeps rates positive while behaving linearly for large <code>eta</code> (unlike <code>exp</code>, which can overflow).  Non-conjugate: the latent <code>v</code> is integrated numerically.</p>"},{"location":"api/#wishart_process_em.NegativeBinomial","title":"wishart_process_em.NegativeBinomial  <code>dataclass</code>","text":"<pre><code>NegativeBinomial()\n</code></pre> <p>               Bases: <code>Likelihood</code></p> <p>Over-dispersed counts <code>y_n ~ NB(mean=softplus(eta_n), r_n)</code>.</p> <p>Uses the mean/dispersion parameterisation with per-neuron dispersion :math:<code>r_n &gt; 0</code>; the variance is :math:<code>\\mu + \\mu^2 / r</code>.  As :math:<code>r \\to \\infty</code> the model approaches :class:<code>Poisson</code>.</p>"},{"location":"api/#wishart_process_em.get_likelihood","title":"wishart_process_em.get_likelihood","text":"<pre><code>get_likelihood(name: str | Likelihood, **kwargs) -&gt; Likelihood\n</code></pre> <p>Look up a likelihood by name (e.g. <code>\"poisson\"</code>) or pass one through.</p> Source code in <code>src/wishart_process_em/likelihoods.py</code> <pre><code>def get_likelihood(name: str | Likelihood, **kwargs) -&gt; Likelihood:\n    \"\"\"Look up a likelihood by name (e.g. ``\"poisson\"``) or pass one through.\"\"\"\n    if isinstance(name, Likelihood):\n        return name\n    key = name.lower()\n    if key not in _REGISTRY:\n        raise ValueError(\n            f\"Unknown likelihood {name!r}; choose from {sorted(_REGISTRY)}\"\n        )\n    return _REGISTRY[key](**kwargs)\n</code></pre>"},{"location":"api/#inference-and-fitting","title":"Inference and fitting","text":""},{"location":"api/#wishart_process_em.fit","title":"wishart_process_em.fit","text":"<p>High-level fitting driver.</p> <p>Maximises the (penalised) marginal log-likelihood</p> <p>.. math::</p> <pre><code>\\mathcal L(\\theta) \\;=\\; \\sum_{c,k} \\log p(y_{ck} \\mid x_c, \\theta)\n                         \\;+\\; \\log p(\\theta),\n</code></pre> <p>with respect to all model parameters using <code>optax</code>.  For the Gaussian model the objective is deterministic (the latent integrates out analytically); for count models each step uses a freshly randomised quasi-Monte-Carlo estimate of the marginal likelihood, giving a stochastic-EM style optimisation.</p>"},{"location":"api/#wishart_process_em.fit.FitResult","title":"FitResult  <code>dataclass</code>","text":"<pre><code>FitResult(params: WPParams, loss_history: ndarray, model: WishartProcessModel, lattice: object = None, init_params: WPParams = None)\n</code></pre> <p>Container for the outcome of :func:<code>fit</code>.</p> <p>Attributes:</p> Name Type Description <code>params</code> <code>WPParams</code> <p>The optimised parameters.</p> <code>loss_history</code> <code>ndarray</code> <p>Objective (negative log-joint) at each optimisation step.</p> <code>model</code> <code>WishartProcessModel</code> <p>The model that was fit (for convenient prediction).</p> <code>lattice</code> <code>QMCLattice or None</code> <p>The QMC lattice used (<code>None</code> for Gaussian models).</p> <code>init_params</code> <code>WPParams</code> <p>The parameters before optimisation.</p>"},{"location":"api/#wishart_process_em.fit.init_bias_from_data","title":"init_bias_from_data","text":"<pre><code>init_bias_from_data(model: WishartProcessModel, Y: ndarray) -&gt; jnp.ndarray\n</code></pre> <p>Initialise the per-neuron mean bias from the data's marginal mean.</p> <p>For Gaussian models this is the empirical mean; for count models it is the inverse-softplus of the empirical mean (so <code>softplus(bias) ~ mean count</code>).</p> Source code in <code>src/wishart_process_em/fit.py</code> <pre><code>def init_bias_from_data(model: WishartProcessModel, Y: jnp.ndarray) -&gt; jnp.ndarray:\n    \"\"\"Initialise the per-neuron mean bias from the data's marginal mean.\n\n    For Gaussian models this is the empirical mean; for count models it is the\n    inverse-softplus of the empirical mean (so ``softplus(bias) ~ mean count``).\n    \"\"\"\n    y_mean = jnp.mean(jnp.asarray(Y), axis=0)\n    if model.likelihood.conjugate_gaussian:\n        return y_mean\n    return softplus_inverse(jnp.maximum(y_mean, 1e-3))\n</code></pre>"},{"location":"api/#wishart_process_em.fit.grand_covariance","title":"grand_covariance","text":"<pre><code>grand_covariance(Y: ndarray, X: ndarray | None = None) -&gt; jnp.ndarray\n</code></pre> <p>Grand empirical covariance (pooled across conditions).</p> <p>If per-trial conditions <code>X</code> are given, trials are centred within each unique condition before pooling (eq. 2 of the paper); otherwise the global covariance is returned.</p> Source code in <code>src/wishart_process_em/fit.py</code> <pre><code>def grand_covariance(Y: jnp.ndarray, X: jnp.ndarray | None = None) -&gt; jnp.ndarray:\n    \"\"\"Grand empirical covariance (pooled across conditions).\n\n    If per-trial conditions ``X`` are given, trials are centred within each\n    unique condition before pooling (eq. 2 of the paper); otherwise the global\n    covariance is returned.\n    \"\"\"\n    Y = np.asarray(Y)\n    if X is None:\n        return jnp.asarray(np.cov(Y, rowvar=False))\n    X = np.asarray(X)\n    keys = X.reshape(X.shape[0], -1)\n    uniq, inv = np.unique(keys, axis=0, return_inverse=True)\n    centered = np.empty_like(Y, dtype=float)\n    for c in range(len(uniq)):\n        mask = inv == c\n        centered[mask] = Y[mask] - Y[mask].mean(axis=0, keepdims=True)\n    return jnp.asarray(np.cov(centered, rowvar=False, bias=True))\n</code></pre>"},{"location":"api/#wishart_process_em.fit.fit","title":"fit","text":"<pre><code>fit(model: WishartProcessModel, Y: ndarray, X: ndarray, num_steps: int = 1000, learning_rate: float = 0.1, seed: int = 0, params0: WPParams | None = None, lattice: QMCLattice | None = None, num_qmc_points: int = 151, method: str = 'auto', optimizer: GradientTransformation | None = None, init_bias: bool = True, init_scale_from_grand_cov: bool = True, progress: bool = True) -&gt; FitResult\n</code></pre> <p>Fit a :class:<code>WishartProcessModel</code> by marginal-likelihood maximisation.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>WishartProcessModel</code> required <code>Y</code> <code>(array, shape(T, N))</code> <p>Trial-by-neuron observations.</p> required <code>X</code> <code>(array, shape(T, D) or (T,))</code> <p>Per-trial condition values, scaled into <code>[0, 1]</code>.</p> required <code>num_steps</code> <code>int</code> <p>Number of optimisation steps.</p> <code>1000</code> <code>learning_rate</code> <code>float</code> <p>Adam step size (ignored if <code>optimizer</code> is given).</p> <code>0.1</code> <code>seed</code> <code>int</code> <p>Base PRNG seed (used to randomise the QMC estimate each step).</p> <code>0</code> <code>params0</code> <code>WPParams</code> <p>Initial parameters; if <code>None</code> they are initialised from <code>model</code> and the data.</p> <code>None</code> <code>lattice</code> <code>QMCLattice</code> <p>Lattice for count models; built automatically if needed.</p> <code>None</code> <code>num_qmc_points</code> <code>int</code> <p>Number of lattice points when building a lattice automatically.</p> <code>151</code> <code>method</code> <code>('auto', 'conjugate', 'qmc', 'laplace')</code> <p>Marginal-likelihood estimator (see :mod:<code>wishart_process_em.inference</code>).</p> <code>\"auto\"</code> <code>optimizer</code> <code>GradientTransformation</code> <p>Custom optimiser; defaults to <code>optax.adam(learning_rate)</code>.</p> <code>None</code> <code>init_bias</code> <code>bool</code> <p>Initialise the mean bias from the data (see :func:<code>init_bias_from_data</code>).</p> <code>True</code> <code>init_scale_from_grand_cov</code> <code>bool</code> <p>When <code>use_scale</code> is enabled, initialise <code>L</code> from the Cholesky of the grand empirical covariance.</p> <code>True</code> <code>progress</code> <code>bool</code> <p>Show a progress bar.</p> <code>True</code> <p>Returns:</p> Type Description <code>FitResult</code> Source code in <code>src/wishart_process_em/fit.py</code> <pre><code>def fit(\n    model: WishartProcessModel,\n    Y: jnp.ndarray,\n    X: jnp.ndarray,\n    num_steps: int = 1000,\n    learning_rate: float = 1e-1,\n    seed: int = 0,\n    params0: WPParams | None = None,\n    lattice: QMCLattice | None = None,\n    num_qmc_points: int = 151,\n    method: str = \"auto\",\n    optimizer: optax.GradientTransformation | None = None,\n    init_bias: bool = True,\n    init_scale_from_grand_cov: bool = True,\n    progress: bool = True,\n) -&gt; FitResult:\n    r\"\"\"Fit a :class:`WishartProcessModel` by marginal-likelihood maximisation.\n\n    Parameters\n    ----------\n    model : WishartProcessModel\n    Y : array, shape (T, N)\n        Trial-by-neuron observations.\n    X : array, shape (T, D) or (T,)\n        Per-trial condition values, scaled into ``[0, 1]``.\n    num_steps : int\n        Number of optimisation steps.\n    learning_rate : float\n        Adam step size (ignored if ``optimizer`` is given).\n    seed : int\n        Base PRNG seed (used to randomise the QMC estimate each step).\n    params0 : WPParams, optional\n        Initial parameters; if ``None`` they are initialised from ``model`` and\n        the data.\n    lattice : QMCLattice, optional\n        Lattice for count models; built automatically if needed.\n    num_qmc_points : int\n        Number of lattice points when building a lattice automatically.\n    method : {\"auto\", \"conjugate\", \"qmc\", \"laplace\"}\n        Marginal-likelihood estimator (see\n        :mod:`wishart_process_em.inference`).\n    optimizer : optax.GradientTransformation, optional\n        Custom optimiser; defaults to ``optax.adam(learning_rate)``.\n    init_bias : bool\n        Initialise the mean bias from the data (see :func:`init_bias_from_data`).\n    init_scale_from_grand_cov : bool\n        When ``use_scale`` is enabled, initialise ``L`` from the Cholesky of the\n        grand empirical covariance.\n    progress : bool\n        Show a progress bar.\n\n    Returns\n    -------\n    FitResult\n    \"\"\"\n    Y = jnp.asarray(Y)\n    X = jnp.asarray(X)\n    X2 = X[:, None] if X.ndim == 1 else X\n\n    conjugate = model.likelihood.conjugate_gaussian\n    if lattice is None and not conjugate:\n        lattice = QMCLattice(num_qmc_points, model.latent_dim, seed=seed)\n\n    # ----- Initialise parameters -----------------------------------------\n    if params0 is None:\n        key = jxr.PRNGKey(seed)\n        mean_bias = init_bias_from_data(model, Y) if init_bias else None\n        scale_tril = None\n        if model.use_scale and init_scale_from_grand_cov:\n            gc = grand_covariance(Y, X2)\n            n = model.num_neurons\n            scale_tril = jnp.linalg.cholesky(gc + model.jitter * jnp.eye(n))\n        params0 = model.init_params(key, mean_bias=mean_bias, scale_tril=scale_tril)\n\n    # ----- Objective ------------------------------------------------------\n    def objective(params, key):\n        ll = dataset_marginal_loglike(\n            model, params, Y, X2, key=key, lattice=lattice, method=method\n        )\n        return -(ll + model.log_prior(params))\n\n    value_and_grad = jax.jit(jax.value_and_grad(objective))\n\n    optimizer = optax.adam(learning_rate) if optimizer is None else optimizer\n    opt_state = optimizer.init(params0)\n\n    params = params0\n    losses = np.empty(num_steps)\n    iterator = trange(num_steps, disable=not progress, desc=\"fit\")\n    for step in iterator:\n        # For the conjugate objective the key is unused; for QMC it randomises\n        # the lattice each step.\n        loss, grads = value_and_grad(params, jxr.PRNGKey(seed + step + 1))\n        updates, opt_state = optimizer.update(grads, opt_state, params)\n        params = optax.apply_updates(params, updates)\n        losses[step] = float(loss)\n        if progress and step % max(1, num_steps // 100) == 0:\n            iterator.set_postfix(loss=f\"{losses[step]:.1f}\")\n\n    return FitResult(\n        params=params,\n        loss_history=losses,\n        model=model,\n        lattice=lattice,\n        init_params=params0,\n    )\n</code></pre>"},{"location":"api/#wishart_process_em.FitResult","title":"wishart_process_em.FitResult  <code>dataclass</code>","text":"<pre><code>FitResult(params: WPParams, loss_history: ndarray, model: WishartProcessModel, lattice: object = None, init_params: WPParams = None)\n</code></pre> <p>Container for the outcome of :func:<code>fit</code>.</p> <p>Attributes:</p> Name Type Description <code>params</code> <code>WPParams</code> <p>The optimised parameters.</p> <code>loss_history</code> <code>ndarray</code> <p>Objective (negative log-joint) at each optimisation step.</p> <code>model</code> <code>WishartProcessModel</code> <p>The model that was fit (for convenient prediction).</p> <code>lattice</code> <code>QMCLattice or None</code> <p>The QMC lattice used (<code>None</code> for Gaussian models).</p> <code>init_params</code> <code>WPParams</code> <p>The parameters before optimisation.</p>"},{"location":"api/#wishart_process_em.dataset_marginal_loglike","title":"wishart_process_em.dataset_marginal_loglike","text":"<pre><code>dataset_marginal_loglike(model, params, Y: ndarray, X: ndarray, key: Array | None = None, lattice=None, method: str = 'auto') -&gt; jnp.ndarray\n</code></pre> <p>Total marginal log-likelihood over a dataset <code>(Y, X)</code>.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>WishartProcessModel</code> required <code>params</code> <code>WPParams</code> required <code>Y</code> <code>ndarray</code> <p>Observations, shape <code>(T, N)</code>.</p> required <code>X</code> <code>ndarray</code> <p>Conditions, shape <code>(T, D)</code> (or <code>(T,)</code> for 1-D).</p> required <code>key</code> <code>Array</code> <p>PRNG key; required for the <code>qmc</code> and <code>laplace</code> methods.</p> <code>None</code> <code>lattice</code> <code>QMCLattice</code> <p>Lattice used for Monte-Carlo integration; required for <code>qmc</code> / <code>laplace</code>.  Its <code>dim</code> must equal <code>model.latent_dim</code>.</p> <code>None</code> <code>method</code> <code>('auto', 'conjugate', 'qmc', 'laplace')</code> <p>Estimator to use.  <code>\"auto\"</code> picks <code>conjugate</code> for Gaussian models and <code>qmc</code> otherwise.</p> <code>\"auto\"</code> Source code in <code>src/wishart_process_em/inference.py</code> <pre><code>def dataset_marginal_loglike(\n    model,\n    params,\n    Y: jnp.ndarray,\n    X: jnp.ndarray,\n    key: jax.Array | None = None,\n    lattice=None,\n    method: str = \"auto\",\n) -&gt; jnp.ndarray:\n    \"\"\"Total marginal log-likelihood over a dataset ``(Y, X)``.\n\n    Parameters\n    ----------\n    model : WishartProcessModel\n    params : WPParams\n    Y : jnp.ndarray\n        Observations, shape ``(T, N)``.\n    X : jnp.ndarray\n        Conditions, shape ``(T, D)`` (or ``(T,)`` for 1-D).\n    key : jax.Array, optional\n        PRNG key; required for the ``qmc`` and ``laplace`` methods.\n    lattice : QMCLattice, optional\n        Lattice used for Monte-Carlo integration; required for ``qmc`` /\n        ``laplace``.  Its ``dim`` must equal ``model.latent_dim``.\n    method : {\"auto\", \"conjugate\", \"qmc\", \"laplace\"}\n        Estimator to use.  ``\"auto\"`` picks ``conjugate`` for Gaussian models and\n        ``qmc`` otherwise.\n    \"\"\"\n    X = jnp.atleast_2d(X.T).T if X.ndim == 1 else jnp.asarray(X)\n    Y = jnp.asarray(Y)\n    resolved = _resolve_method(model, method)\n\n    if resolved == \"conjugate\":\n        vals = jax.vmap(lambda y, x: marginal_loglike_point_conjugate(model, params, y, x))(\n            Y, X\n        )\n        return jnp.sum(vals)\n\n    if lattice is None:\n        raise ValueError(f\"method={resolved!r} requires a QMCLattice\")\n    if lattice.dim != model.latent_dim:\n        raise ValueError(\n            f\"lattice.dim ({lattice.dim}) must equal model.latent_dim \"\n            f\"({model.latent_dim})\"\n        )\n    if key is None:\n        raise ValueError(f\"method={resolved!r} requires a PRNG key\")\n\n    point_fn = {\n        \"qmc\": marginal_loglike_point_qmc,\n        \"laplace\": marginal_loglike_point_laplace,\n    }[resolved]\n\n    keys = jxr.split(key, Y.shape[0])\n    vals = jax.vmap(lambda y, x, k: point_fn(model, params, y, x, k, lattice))(Y, X, keys)\n    return jnp.sum(vals)\n</code></pre>"},{"location":"api/#wishart_process_em.marginal_loglike_point_conjugate","title":"wishart_process_em.marginal_loglike_point_conjugate","text":"<pre><code>marginal_loglike_point_conjugate(model, params, y, x) -&gt; jnp.ndarray\n</code></pre> <p>Exact log marginal likelihood for a conjugate (Gaussian) likelihood.</p> Source code in <code>src/wishart_process_em/inference.py</code> <pre><code>def marginal_loglike_point_conjugate(model, params, y, x) -&gt; jnp.ndarray:\n    \"\"\"Exact log marginal likelihood for a conjugate (Gaussian) likelihood.\"\"\"\n    mu = model.mean_at(params, x)\n    cov = model.cov_at(params, x)\n    return model.likelihood.marginal_log_prob(params.lik, y, mu, cov)\n</code></pre>"},{"location":"api/#wishart_process_em.marginal_loglike_point_qmc","title":"wishart_process_em.marginal_loglike_point_qmc","text":"<pre><code>marginal_loglike_point_qmc(model, params, y, x, key, lattice) -&gt; jnp.ndarray\n</code></pre> <p>Randomised-QMC estimate of the log marginal likelihood for one trial.</p> <p>Draws latent samples <code>v</code> from the prior via the lattice, forms <code>eta = mu + G v</code> and returns <code>logsumexp_s log p(y | eta_s) - log S</code>.</p> Source code in <code>src/wishart_process_em/inference.py</code> <pre><code>def marginal_loglike_point_qmc(model, params, y, x, key, lattice) -&gt; jnp.ndarray:\n    r\"\"\"Randomised-QMC estimate of the log marginal likelihood for one trial.\n\n    Draws latent samples ``v`` from the prior via the lattice, forms\n    ``eta = mu + G v`` and returns ``logsumexp_s log p(y | eta_s) - log S``.\n    \"\"\"\n    mu = model.mean_at(params, x)\n    g = model.sqrt_factor_at(params, x)  # (N, Q)\n    v = lattice.gaussian_points(key)  # (S, Q)\n    eta = mu[None, :] + v @ g.T  # (S, N)\n    log_p = jax.vmap(lambda e: model.likelihood.log_prob(params.lik, y, e))(eta)\n    return jax.scipy.special.logsumexp(log_p) - jnp.log(v.shape[0])\n</code></pre>"},{"location":"api/#wishart_process_em.marginal_loglike_point_laplace","title":"wishart_process_em.marginal_loglike_point_laplace","text":"<pre><code>marginal_loglike_point_laplace(model, params, y, x, key, lattice) -&gt; jnp.ndarray\n</code></pre> <p>Laplace-proposal multiple-importance-sampling estimate for one trial.</p> <p>The proposal is an equal mixture of the prior <code>N(0, I)</code> and the Laplace approximation <code>N(m, H^{-1})</code> to the latent posterior, where <code>m</code> is the posterior mode and <code>H</code> its Hessian.  Uses the balance heuristic across the two components.</p> Source code in <code>src/wishart_process_em/inference.py</code> <pre><code>def marginal_loglike_point_laplace(model, params, y, x, key, lattice) -&gt; jnp.ndarray:\n    r\"\"\"Laplace-proposal multiple-importance-sampling estimate for one trial.\n\n    The proposal is an equal mixture of the prior ``N(0, I)`` and the Laplace\n    approximation ``N(m, H^{-1})`` to the latent posterior, where ``m`` is the\n    posterior mode and ``H`` its Hessian.  Uses the balance heuristic across the\n    two components.\n    \"\"\"\n    mu = model.mean_at(params, x)\n    g = model.sqrt_factor_at(params, x)  # (N, Q)\n    q_dim = g.shape[1]\n\n    def neg_joint(v):\n        eta = mu + g @ v\n        log_lik = model.likelihood.log_prob(params.lik, y, eta)\n        log_prior = jnp.sum(jax.scipy.stats.norm.logpdf(v))\n        return -(log_lik + log_prior)\n\n    res = newton_minimize(neg_joint, jnp.zeros(q_dim))\n    m, lh = res.x, res.hess_chol  # H = lh @ lh.T ; Cov = H^{-1}\n\n    k_prior, k_lap = jxr.split(key)\n    v_prior = lattice.gaussian_points(k_prior)  # (S, Q) ~ N(0, I)\n    eps = lattice.gaussian_points(k_lap)  # (S, Q)\n    # v = m + lh^{-T} eps  -&gt;  Cov(v) = (lh lh^T)^{-1} = H^{-1}\n    v_lap = m[None, :] + jax.scipy.linalg.solve_triangular(lh.T, eps.T, lower=False).T\n    v_all = jnp.concatenate([v_prior, v_lap], axis=0)  # (2S, Q)\n\n    # Component densities.\n    log_prior = jnp.sum(jax.scipy.stats.norm.logpdf(v_all), axis=1)\n    diff = v_all - m[None, :]\n    quad = jnp.sum((diff @ lh) ** 2, axis=1)  # (v-m)^T H (v-m)\n    log_det_cov = -2.0 * jnp.sum(jnp.log(jnp.diagonal(lh)))\n    log_lap = -0.5 * (q_dim * _LOG_2PI + log_det_cov + quad)\n\n    # Mixture proposal density (balance heuristic).\n    log_q = jnp.logaddexp(jnp.log(0.5) + log_prior, jnp.log(0.5) + log_lap)\n\n    eta_all = mu[None, :] + v_all @ g.T\n    log_lik = jax.vmap(lambda e: model.likelihood.log_prob(params.lik, y, e))(eta_all)\n\n    log_weights = log_lik + log_prior - log_q\n    return jax.scipy.special.logsumexp(log_weights) - jnp.log(v_all.shape[0])\n</code></pre>"},{"location":"api/#wishart_process_em.init_bias_from_data","title":"wishart_process_em.init_bias_from_data","text":"<pre><code>init_bias_from_data(model: WishartProcessModel, Y: ndarray) -&gt; jnp.ndarray\n</code></pre> <p>Initialise the per-neuron mean bias from the data's marginal mean.</p> <p>For Gaussian models this is the empirical mean; for count models it is the inverse-softplus of the empirical mean (so <code>softplus(bias) ~ mean count</code>).</p> Source code in <code>src/wishart_process_em/fit.py</code> <pre><code>def init_bias_from_data(model: WishartProcessModel, Y: jnp.ndarray) -&gt; jnp.ndarray:\n    \"\"\"Initialise the per-neuron mean bias from the data's marginal mean.\n\n    For Gaussian models this is the empirical mean; for count models it is the\n    inverse-softplus of the empirical mean (so ``softplus(bias) ~ mean count``).\n    \"\"\"\n    y_mean = jnp.mean(jnp.asarray(Y), axis=0)\n    if model.likelihood.conjugate_gaussian:\n        return y_mean\n    return softplus_inverse(jnp.maximum(y_mean, 1e-3))\n</code></pre>"},{"location":"api/#wishart_process_em.grand_covariance","title":"wishart_process_em.grand_covariance","text":"<pre><code>grand_covariance(Y: ndarray, X: ndarray | None = None) -&gt; jnp.ndarray\n</code></pre> <p>Grand empirical covariance (pooled across conditions).</p> <p>If per-trial conditions <code>X</code> are given, trials are centred within each unique condition before pooling (eq. 2 of the paper); otherwise the global covariance is returned.</p> Source code in <code>src/wishart_process_em/fit.py</code> <pre><code>def grand_covariance(Y: jnp.ndarray, X: jnp.ndarray | None = None) -&gt; jnp.ndarray:\n    \"\"\"Grand empirical covariance (pooled across conditions).\n\n    If per-trial conditions ``X`` are given, trials are centred within each\n    unique condition before pooling (eq. 2 of the paper); otherwise the global\n    covariance is returned.\n    \"\"\"\n    Y = np.asarray(Y)\n    if X is None:\n        return jnp.asarray(np.cov(Y, rowvar=False))\n    X = np.asarray(X)\n    keys = X.reshape(X.shape[0], -1)\n    uniq, inv = np.unique(keys, axis=0, return_inverse=True)\n    centered = np.empty_like(Y, dtype=float)\n    for c in range(len(uniq)):\n        mask = inv == c\n        centered[mask] = Y[mask] - Y[mask].mean(axis=0, keepdims=True)\n    return jnp.asarray(np.cov(centered, rowvar=False, bias=True))\n</code></pre>"},{"location":"api/#numerics","title":"Numerics","text":""},{"location":"api/#wishart_process_em.QMCLattice","title":"wishart_process_em.QMCLattice","text":"<pre><code>QMCLattice(num_points: int, dim: int, generator: int | None = None, seed: int = 0)\n</code></pre> <p>A randomised Korobov lattice for integrating standard-normal latents.</p> <p>Parameters:</p> Name Type Description Default <code>num_points</code> <code>int</code> <p>Number of lattice points <code>n</code> (ideally prime).</p> required <code>dim</code> <code>int</code> <p>Latent dimension <code>Q</code>.</p> required <code>generator</code> <code>int</code> <p>Korobov generator <code>a</code>.  If <code>None</code>, it is selected by :func:<code>find_optimal_generator</code>.</p> <code>None</code> <code>seed</code> <code>int</code> <p>Seed for the (host-side) generator search, for reproducibility.</p> <code>0</code> Source code in <code>src/wishart_process_em/qmc.py</code> <pre><code>def __init__(\n    self,\n    num_points: int,\n    dim: int,\n    generator: int | None = None,\n    seed: int = 0,\n) -&gt; None:\n    self.num_points = int(num_points)\n    self.dim = int(dim)\n    rng = np.random.default_rng(seed)\n    if generator is None:\n        generator, disc = find_optimal_generator(self.num_points, self.dim, rng=rng)\n        self.discrepancy = disc\n    else:\n        self.discrepancy = star_discrepancy(\n            self.num_points, generator, self.dim, rng=rng\n        )\n    self.generator = int(generator)\n    self.points = jnp.asarray(korobov_points(self.num_points, self.generator, self.dim))\n</code></pre>"},{"location":"api/#wishart_process_em.QMCLattice.gaussian_points","title":"gaussian_points","text":"<pre><code>gaussian_points(key: Array) -&gt; jnp.ndarray\n</code></pre> <p>Randomised QMC samples from :math:<code>\\mathcal N(0, I_{\\text{dim}})</code>.</p> <p>Applies a random shift (mod 1) and a random orthogonal rotation, then maps through the inverse Gaussian CDF.  Returns an <code>(num_points, dim)</code> array whose empirical distribution approximates a standard normal but with much lower integration error than i.i.d. sampling.</p> Source code in <code>src/wishart_process_em/qmc.py</code> <pre><code>def gaussian_points(self, key: jax.Array) -&gt; jnp.ndarray:\n    \"\"\"Randomised QMC samples from :math:`\\\\mathcal N(0, I_{\\\\text{dim}})`.\n\n    Applies a random shift (mod 1) and a random orthogonal rotation, then\n    maps through the inverse Gaussian CDF.  Returns an ``(num_points, dim)``\n    array whose empirical distribution approximates a standard normal but\n    with much lower integration error than i.i.d. sampling.\n    \"\"\"\n    k_shift, k_rot = jxr.split(key)\n    shift = jxr.uniform(k_shift, shape=(1, self.dim))\n    rotation = jxr.orthogonal(k_rot, self.dim)\n    shifted = (self.points + shift) % 1.0\n    # ndtri = inverse standard-normal CDF.\n    z = jax.scipy.special.ndtri(shifted)\n    return z @ rotation\n</code></pre>"},{"location":"api/#wishart_process_em.newton_minimize","title":"wishart_process_em.newton_minimize","text":"<pre><code>newton_minimize(fun: Callable[[ndarray], ndarray], x0: ndarray, max_iter: int = 50, tol: float = 0.0001, damping: float = 1e-06, ls_alpha: float = 0.3, ls_beta: float = 0.8, ls_min_step: float = 1e-12) -&gt; NewtonResult\n</code></pre> <p>Minimise a smooth scalar function with damped Newton + line search.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <code>Callable</code> <p>Scalar objective <code>f : R^d -&gt; R</code> (traceable by JAX).</p> required <code>x0</code> <code>ndarray</code> <p>Initial point of shape <code>(d,)</code>.</p> required <code>max_iter</code> <code>int</code> <p>Maximum Newton iterations.</p> <code>50</code> <code>tol</code> <code>float</code> <p>Stop when <code>||grad|| &lt; sqrt(d) * tol</code>.</p> <code>0.0001</code> <code>damping</code> <code>float</code> <p>Levenberg-Marquardt damping added to the Hessian diagonal for positive definiteness (on top of any curvature from the prior).</p> <code>1e-06</code> <code>ls_alpha</code> <code>float</code> <p>Armijo line-search parameters.</p> <code>0.3</code> <code>ls_beta</code> <code>float</code> <p>Armijo line-search parameters.</p> <code>0.3</code> <code>ls_min_step</code> <code>float</code> <p>Armijo line-search parameters.</p> <code>0.3</code> <p>Returns:</p> Type Description <code>NewtonResult</code> Source code in <code>src/wishart_process_em/optim.py</code> <pre><code>def newton_minimize(\n    fun: Callable[[jnp.ndarray], jnp.ndarray],\n    x0: jnp.ndarray,\n    max_iter: int = 50,\n    tol: float = 1e-4,\n    damping: float = 1e-6,\n    ls_alpha: float = 0.3,\n    ls_beta: float = 0.8,\n    ls_min_step: float = 1e-12,\n) -&gt; NewtonResult:\n    r\"\"\"Minimise a smooth scalar function with damped Newton + line search.\n\n    Parameters\n    ----------\n    fun : Callable\n        Scalar objective ``f : R^d -&gt; R`` (traceable by JAX).\n    x0 : jnp.ndarray\n        Initial point of shape ``(d,)``.\n    max_iter : int, optional\n        Maximum Newton iterations.\n    tol : float, optional\n        Stop when ``||grad|| &lt; sqrt(d) * tol``.\n    damping : float, optional\n        Levenberg-Marquardt damping added to the Hessian diagonal for positive\n        definiteness (on top of any curvature from the prior).\n    ls_alpha, ls_beta, ls_min_step : float, optional\n        Armijo line-search parameters.\n\n    Returns\n    -------\n    NewtonResult\n    \"\"\"\n    grad_fun = jax.grad(fun)\n    hess_fun = jax.hessian(fun)\n    d = x0.shape[0]\n    eye = jnp.eye(d)\n    tol_scaled = jnp.sqrt(d) * tol\n\n    def cond(state):\n        x, g, i, _ = state\n        return jnp.logical_and(i &lt; max_iter, jnp.linalg.norm(g) &gt;= tol_scaled)\n\n    def body(state):\n        x, g, i, _ = state\n        fx = fun(x)\n        H = hess_fun(x) + damping * eye\n        L = jnp.linalg.cholesky(H)\n        p = jax.scipy.linalg.cho_solve((L, True), -g)\n        t = _line_search(fun, x, fx, g, p, ls_alpha, ls_beta, ls_min_step)\n        x_new = x + t * p\n        return (x_new, grad_fun(x_new), i + 1, L)\n\n    g0 = grad_fun(x0)\n    L0 = jnp.linalg.cholesky(hess_fun(x0) + damping * eye)\n    x, g, n_iter, L = jax.lax.while_loop(cond, body, (x0, g0, jnp.asarray(0), L0))\n\n    grad_norm = jnp.linalg.norm(g)\n    return NewtonResult(\n        x=x,\n        grad_norm=grad_norm,\n        n_iter=n_iter,\n        hess_chol=L,\n        converged=grad_norm &lt; tol_scaled,\n    )\n</code></pre>"},{"location":"api/#wishart_process_em.NewtonResult","title":"wishart_process_em.NewtonResult","text":"<p>               Bases: <code>NamedTuple</code></p> <p>Result of :func:<code>newton_minimize</code>.</p> <p>Attributes:</p> Name Type Description <code>x</code> <code>ndarray</code> <p>The (approximate) minimiser / posterior mode.</p> <code>grad_norm</code> <code>ndarray</code> <p>Euclidean norm of the gradient at <code>x</code>.</p> <code>n_iter</code> <code>ndarray</code> <p>Number of Newton iterations taken.</p> <code>hess_chol</code> <code>ndarray</code> <p>Lower-triangular Cholesky factor <code>L</code> of the (damped) Hessian at <code>x</code>, with <code>L @ L.T</code> approximately equal to the Hessian.</p> <code>converged</code> <code>ndarray</code> <p>Boolean flag; <code>True</code> if the gradient-norm tolerance was met.</p>"},{"location":"cli/","title":"Command-line interface","text":"<p>Installing the package provides the <code>wishart-em</code> command-line tool for fitting, evaluating, and sampling Wishart process models without writing Python. It wraps the same <code>fit</code> driver and model API used from code.</p> <p>Illustrative flags</p> <p>The commands and flags below are illustrative. The authoritative, always up-to-date list of subcommands and options is:</p> <pre><code>wishart-em --help\nwishart-em fit --help          # per-subcommand help\n</code></pre>"},{"location":"cli/#data-format","title":"Data format","text":"<p>All subcommands read and write NumPy <code>.npz</code> archives containing two arrays:</p> Array Shape Meaning <code>Y</code> <code>(T, N)</code> Trial-by-neuron observations (real-valued for Gaussian, counts for Poisson / negative binomial). <code>X</code> <code>(T, D)</code> Per-trial condition values, scaled into <code>[0, 1]^D</code>. A 1-D condition may be stored as <code>(T,)</code>. <p>Create one from Python with <code>numpy.savez</code>:</p> <pre><code>import numpy as np\nnp.savez(\"data.npz\", Y=Y, X=X)\n</code></pre>"},{"location":"cli/#fit-fit-a-model-to-data","title":"<code>fit</code> \u2014 fit a model to data","text":"<p>Fit a Wishart process to a dataset and save the resulting parameters.</p> <pre><code>wishart-em fit data.npz --likelihood poisson --rank 3 --steps 1000 -o fit.npz\n</code></pre> <p>Common options:</p> Flag Meaning <code>--likelihood {gaussian,poisson,negative_binomial}</code> Observation model. Gaussian uses the exact (conjugate) marginal likelihood; count models use QMC. <code>--rank P</code> Rank \\(P\\) of the low-rank covariance factor \\(U\\). <code>--steps N</code> Number of optimisation steps. <code>-o, --output fit.npz</code> Where to write the fitted model / parameters. <p>The saved <code>fit.npz</code> bundles the optimised parameters together with the model configuration (basis, likelihood, rank, \u2026) so the <code>evaluate</code> and <code>sample</code> subcommands can reconstruct the model.</p>"},{"location":"cli/#evaluate-held-out-log-likelihood","title":"<code>evaluate</code> \u2014 held-out log-likelihood","text":"<p>Score a saved fit on a held-out dataset, reporting the marginal log-likelihood (using the more accurate Laplace estimator for count models):</p> <pre><code>wishart-em evaluate fit.npz heldout.npz\n</code></pre> <p>This is the primary quantitative check that the model generalises across conditions rather than overfitting the training trials.</p>"},{"location":"cli/#sample-generate-synthetic-data","title":"<code>sample</code> \u2014 generate synthetic data","text":"<p>Draw synthetic trials from a fitted model, e.g. for posterior predictive checks or downstream simulation:</p> <pre><code>wishart-em sample fit.npz --trials 500 -o synthetic.npz\n</code></pre> <p>The output <code>synthetic.npz</code> again holds <code>Y</code> (the sampled observations) and <code>X</code> (the conditions they were sampled at), matching the input data format.</p>"},{"location":"cli/#typical-workflow","title":"Typical workflow","text":"<pre><code># 1. Fit on the training split.\nwishart-em fit train.npz --likelihood negative_binomial --rank 3 --steps 1500 -o fit.npz\n\n# 2. Check held-out generalisation.\nwishart-em evaluate fit.npz test.npz\n\n# 3. Generate synthetic data for a predictive check.\nwishart-em sample fit.npz --trials 1000 -o synthetic.npz\n</code></pre> <p>See <code>wishart-em --help</code> for the complete and current option list.</p>"},{"location":"model/","title":"The model","text":"<p>This page gives the full mathematical description of the finite-basis Wishart process implemented by <code>wishart-process-em</code>. It follows the model of Nejatbakhsh, Garon &amp; Williams (2023) (\"Estimating Noise Correlations Across Continuous Conditions With Wishart Processes\", NeurIPS 2023), with a finite Fourier-feature basis and an EM-style, marginal-likelihood inference in place of variational inference.</p>"},{"location":"model/#problem-setup","title":"Problem setup","text":"<p>We record the activity of \\(N\\) neurons across \\(C\\) experimental conditions indexed by a continuous parameter \\(x \\in [0,1]^D\\) (e.g. grating orientation, reach angle, tone frequency). At each condition we observe a small number \\(K\\) of trials. Writing \\(y_{ck} \\in \\mathbb{R}^N\\) for trial \\(k\\) at condition \\(c\\), the object of interest is the noise covariance</p> \\[ \\Sigma(x) \\;=\\; \\operatorname{Cov}\\!\\big[\\, y \\mid x \\,\\big], \\] <p>i.e. the trial-to-trial (co)variability of the population at each condition.</p> <p>The statistical difficulty is the \\(K &lt; N\\) regime: with fewer trials per condition than neurons, the per-condition sample covariance is rank-deficient and badly conditioned, so it cannot be estimated condition-by-condition. The Wishart process resolves this by assuming \\(\\Sigma(x)\\) varies smoothly in \\(x\\), so trials at neighbouring conditions inform each other and statistical power is pooled across conditions.</p>"},{"location":"model/#generative-model","title":"Generative model","text":"<p>Each trial is generated by first drawing a low-dimensional standard-normal latent, mapping it through a condition-dependent mean and covariance square root, and finally passing the result through an observation model:</p> \\[ v \\sim \\mathcal N(0, I_Q), \\qquad \\eta \\;=\\; \\mu(x) + G(x)\\, v, \\qquad y \\sim p(\\,\\cdot \\mid \\eta), \\] <p>where \\(\\eta \\in \\mathbb{R}^N\\) is a latent linear predictor, \\(\\mu(x)\\) is a smooth mean function, and \\(G(x) \\in \\mathbb{R}^{N \\times Q}\\) is a covariance square root satisfying</p> \\[ G(x)\\, G(x)^\\top \\;=\\; \\Sigma(x). \\] <p>For the Gaussian observation model the latent \\(v\\) integrates out analytically and \\(y \\sim \\mathcal N(\\mu(x), \\Sigma(x))\\) exactly; for count models the latent is integrated numerically (see below).</p>"},{"location":"model/#weight-space-gaussian-process-basis","title":"Weight-space Gaussian-process basis","text":"<p>Both the mean function \\(\\mu(x)\\) and the covariance factors are built from scalar Gaussian processes. Rather than working with a full kernel matrix, each scalar GP is approximated in weight space by a truncated Fourier expansion:</p> \\[ f(x) \\;=\\; \\sum_{k} \\tau_k \\big( w^{\\sin}_k \\sin(2\\pi\\, k \\cdot x)                                + w^{\\cos}_k \\cos(2\\pi\\, k \\cdot x) \\big), \\qquad w \\sim \\mathcal N(0, 1), \\] <p>where the integer frequency vectors \\(k\\) range over a grid (excluding the constant mode, which is carried by an explicit bias term) and the feature amplitudes are</p> \\[ \\tau_k \\;=\\; \\sqrt{S(2\\pi k)}, \\] <p>with \\(S(\\omega)\\) the spectral density of the desired kernel. By Bochner's theorem, a stationary kernel \\(k(x,x')\\) has a spectral density given by the Fourier transform of \\(k\\); weighting each Fourier feature by \\(\\sqrt{S(\\omega)}\\) and placing a standard-normal prior on the weights therefore reproduces the corresponding stationary GP prior on \\(f\\). Two spectral densities are provided:</p> <ul> <li>Squared-exponential (RBF): \\(S(\\omega) \\propto \\exp(-\\tfrac12 \\ell^2   |\\omega|^2)\\). The length scale \\(\\ell\\) is the smoothness / bandwidth   hyperparameter (the analogue of \\(\\lambda\\) in the paper).</li> <li>Mat\u00e9rn with smoothness \\(\\nu\\): \\(S(\\omega) \\propto (2\\nu/\\ell^2 +   |\\omega|^2)^{-(\\nu + 1/2)}\\); \\(\\nu \\to \\infty\\) recovers the squared   exponential.</li> </ul> <p>Working in weight space turns the otherwise nonparametric GP into a finite, differentiable parametric model whose free parameters are just the weights \\(w\\), on which we place a standard-normal prior. This is exactly what makes the marginal-likelihood / EM inference tractable.</p> <p>Periodicity</p> <p>Inputs \\(x\\) are assumed scaled to the unit box \\([0,1]^D\\). Because the features are Fourier modes on the torus, periodic conditions (grating orientation, reach angle) wrap around naturally. Non-periodic conditions should be mapped into \\([0,1]\\) with a little headroom at the edges.</p>"},{"location":"model/#the-covariance-family","title":"The covariance family","text":"<p>The covariance follows the full family of the paper (sec. 2.3):</p> \\[ \\Sigma(x) \\;=\\; L\\,\\big( U(x)\\, U(x)^\\top + \\Lambda(x) \\big)\\, L^\\top, \\] <p>with three ingredients, each optional beyond the low-rank core:</p> <ul> <li>Low-rank factor \\(U(x) \\in \\mathbb{R}^{N \\times P}\\). Its columns are   weight-space GPs; column \\(p\\) is scaled by a learnable per-latent (ARD) scale   \\(s^Z_p = \\operatorname{softplus}(\\cdot) &gt; 0\\), so \\(U(x) = A(x)\\,   \\operatorname{diag}(s^Z)\\) with \\(A(x)\\) the raw GP factor. The rank \\(P\\) controls   how many shared covariance modes are modelled.</li> <li>Diagonal term \\(\\Lambda(x) = \\operatorname{diag}(\\lambda(x))\\), optional   (the low-rank plus diagonal / <code>WPlrd</code> extension). Each entry   \\(\\lambda_n(x) = \\operatorname{softplus}(g_n(x)) \\ge 0\\) is a   softplus-transformed GP, adding per-neuron heteroscedastic variance.</li> <li>Scale matrix \\(L\\), an optional learnable lower-triangular matrix capturing   condition-independent covariance. It is typically initialised from the   Cholesky factor of the grand empirical covariance (pooled, within-condition   centred). Its diagonal is passed through softplus to guarantee a valid   Cholesky factor.</li> </ul> <p>For sampling and for the non-Gaussian marginal likelihood we use the square-root factor \\(G(x)\\) directly:</p> \\[ G(x) \\;=\\; L \\begin{bmatrix} U(x) &amp; \\operatorname{diag}\\!\\big(\\sqrt{\\lambda(x)}\\big) \\end{bmatrix}, \\qquad \\Sigma(x) = G(x) G(x)^\\top + \\epsilon I, \\] <p>with a small jitter \\(\\epsilon\\) for numerical stability. The latent dimension is</p> \\[ Q \\;=\\; P \\;+\\; \\begin{cases} N &amp; \\text{if the diagonal term is used,} \\\\ 0 &amp; \\text{otherwise.} \\end{cases} \\] <p>When the diagonal is disabled, \\(Q = P\\) is small, which is what keeps the QMC latent integral efficient for count models.</p>"},{"location":"model/#observation-models","title":"Observation models","text":"<p>Given the latent predictor \\(\\eta = \\mu(x) + G(x)\\,v\\), a likelihood maps \\(\\eta\\) to observations \\(y\\). Three models are provided.</p>"},{"location":"model/#gaussian-conjugate","title":"Gaussian (conjugate)","text":"\\[ y \\sim \\mathcal N\\big(\\mu(x),\\; \\Sigma(x)\\big). \\] <p>This is conjugate: the latent \\(v\\) integrates out in closed form, so the marginal likelihood is exactly a multivariate normal and no Monte-Carlo is required. An optional homoscedastic observation-noise variance \\(\\sigma^2_{\\text{obs}}\\) can be added on the diagonal of \\(\\Sigma(x)\\). This is the model used throughout the original paper on (pre-processed) neural data.</p>"},{"location":"model/#poisson","title":"Poisson","text":"\\[ y_n \\sim \\operatorname{Poisson}\\big(\\operatorname{softplus}(\\eta_n)\\big). \\] <p>The softplus inverse-link keeps rates positive while behaving linearly for large \\(\\eta\\) (unlike \\(\\exp\\), which can overflow). Suited to raw spike counts. Non-conjugate: the latent \\(v\\) must be integrated numerically.</p>"},{"location":"model/#negative-binomial","title":"Negative binomial","text":"\\[ y_n \\sim \\operatorname{NB}\\big(\\text{mean} = \\operatorname{softplus}(\\eta_n),\\; r_n\\big), \\] <p>with a per-neuron dispersion \\(r_n &gt; 0\\) (a weak standard-normal prior is placed on \\(\\log r_n\\)). The mean/dispersion parameterisation gives variance \\(\\mu + \\mu^2/r\\); as \\(r \\to \\infty\\) the model approaches the Poisson. Captures over-dispersed counts. Also non-conjugate.</p>"},{"location":"model/#marginal-likelihood-estimators","title":"Marginal-likelihood estimators","text":"<p>Fitting maximises the marginal likelihood, in which the per-trial latent \\(v\\) is integrated out:</p> \\[ p(y \\mid x, \\theta) \\;=\\; \\int p\\big(y \\mid \\mu(x) + G(x)\\,v\\big)\\,                                \\mathcal N(v; 0, I_Q)\\, \\mathrm d v . \\] <p>The full objective is the penalised marginal log-likelihood over the dataset,</p> \\[ \\mathcal L(\\theta) \\;=\\; \\sum_{c,k} \\log p(y_{ck} \\mid x_c, \\theta)                          \\;+\\; \\log p(\\theta), \\] <p>where \\(\\log p(\\theta)\\) is the standard-normal prior on the GP weights (plus any likelihood-parameter prior). It is maximised with Adam via <code>optax</code>. Three per-trial estimators of \\(\\log p(y \\mid x, \\theta)\\) are available.</p>"},{"location":"model/#conjugate","title":"Conjugate","text":"<p>For the Gaussian likelihood the integral is exact,</p> \\[ \\log p(y \\mid x, \\theta) \\;=\\; \\log \\mathcal N\\big(y;\\; \\mu(x),\\; \\Sigma(x) + \\sigma^2_{\\text{obs}} I\\big), \\] <p>so the objective is deterministic and no lattice is needed.</p>"},{"location":"model/#qmc","title":"QMC","text":"<p>For count models we draw the latent \\(v\\) from the prior using a randomised rank-1 Korobov lattice, form \\(\\eta_s = \\mu(x) + G(x)\\, v_s\\), and take an importance-weighted (IWAE-style) estimate of the log marginal:</p> \\[ \\widehat{\\log p}(y \\mid x, \\theta)    \\;=\\; \\operatorname*{logsumexp}_{s=1}^{S} \\log p(y \\mid \\eta_s) \\;-\\; \\log S . \\] <p>The lattice is deterministic on the unit cube but at integration time receives a random shift (mod 1), a random rotation, and is mapped to standard normals via the inverse Gaussian CDF; this makes the estimator unbiased after randomisation while converging markedly faster than plain Monte-Carlo for smooth integrands. A fresh randomisation each step gives a stochastic-EM\u2013style optimisation. This is the cheap, differentiable objective used during training.</p>"},{"location":"model/#laplace","title":"Laplace","text":"<p>For accurate evaluation (e.g. held-out log-likelihood) a lower-variance multiple-importance-sampling estimate is available. Its proposal is an equal mixture of the prior \\(\\mathcal N(0, I)\\) and a Laplace approximation \\(\\mathcal N(m, H^{-1})\\) to the latent posterior, where the mode \\(m\\) and Hessian \\(H\\) come from a per-trial damped-Newton solve; samples are combined with the balance heuristic. This costs a Newton solve per trial, so it is intended for evaluation rather than every gradient step. The Newton routine returns the Cholesky factor \\(L\\) of \\(H\\) (\\(L L^\\top = H\\)), so a Laplace sample is \\(m + L^{-\\top}\\varepsilon\\) with \\(\\varepsilon \\sim \\mathcal N(0, I)\\).</p>"},{"location":"model/#references","title":"References","text":"<ul> <li>Amin Nejatbakhsh, Isabel Garon, and Alex H. Williams. Estimating Noise   Correlations Across Continuous Conditions With Wishart Processes. Advances in   Neural Information Processing Systems (NeurIPS), 2023.   arXiv:2308.11824.</li> </ul>"},{"location":"quickstart/","title":"Quickstart walkthrough","text":"<p>This page walks through a complete workflow: simulate Gaussian data from a known Wishart process, fit a model, predict smooth condition-dependent covariance, and visualise it as covariance ellipses in PCA space. It then shows the Poisson (spike-count) variant and its identifiability caveat.</p> <p>For the mathematical background behind each step, see the model page.</p>"},{"location":"quickstart/#1-a-smooth-gp-basis","title":"1. A smooth GP basis","text":"<p>Every latent function in the model is a weight-space GP built on a truncated Fourier basis. Here we model a single periodic condition \\(x \\in [0,1]\\) (think grating orientation mapped so that \\(0\\) and \\(1\\) coincide):</p> <pre><code>import jax.numpy as jnp, jax.random as jxr\nfrom wishart_process_em import (\n    TruncatedFourierBasis, WishartProcessModel, fit, squared_exponential,\n)\n\nbasis = TruncatedFourierBasis(\n    max_freq=8, num_dims=1,\n    spectral_density=squared_exponential(lengthscale=0.2),\n)\n</code></pre> <p>A smaller <code>lengthscale</code> admits higher frequencies and yields rougher covariance functions; <code>max_freq</code> caps the number of Fourier modes per input dimension. For a rougher prior you can swap in <code>matern(lengthscale=0.2, nu=1.5)</code>.</p>"},{"location":"quickstart/#2-simulate-gaussian-data","title":"2. Simulate Gaussian data","text":"<p>We instantiate a Gaussian Wishart process over \\(N = 25\\) neurons with a rank \\(P = 3\\) covariance factor, then sample trials from its generative model. Sampling draws a latent \\(v \\sim \\mathcal N(0, I_Q)\\) per trial, forms \\(\\eta = \\mu(x) + G(x)\\,v\\), and (for the Gaussian model) returns \\(y = \\eta\\):</p> <pre><code>model = WishartProcessModel(basis, num_neurons=25, rank=3, likelihood=\"gaussian\")\n\ntrue_params = model.init_params(jxr.PRNGKey(0))\nX = jnp.linspace(0, 1, 400)                       # 400 trials, condition in [0,1]\nY, _ = model.sample(jxr.PRNGKey(1), true_params, X)   # Y has shape (400, 25)\n</code></pre> <p>In a real analysis, <code>Y</code> is your trial-by-neuron response matrix (shape <code>(T, N)</code>) and <code>X</code> your per-trial condition values, scaled into <code>[0, 1]</code>.</p>"},{"location":"quickstart/#3-fit-the-model","title":"3. Fit the model","text":"<p><code>fit</code> maximises the penalised marginal log-likelihood with Adam. For the Gaussian likelihood the objective is exact (the latent integrates out), so no Monte-Carlo is involved and the fit is deterministic:</p> <pre><code>result = fit(model, Y, X, num_steps=500, learning_rate=1e-1)\n</code></pre> <p><code>result</code> is a <code>FitResult</code> bundling the optimised <code>params</code>, the <code>loss_history</code> (negative log-joint per step, handy for a convergence plot), the <code>model</code>, and \u2014 for count models \u2014 the QMC <code>lattice</code>. Plot <code>result.loss_history</code> to confirm the objective has plateaued.</p>"},{"location":"quickstart/#4-predict-smooth-covariance","title":"4. Predict smooth covariance","text":"<p><code>predict_cov</code> evaluates \\(\\Sigma(x)\\) on a grid of conditions. For the Gaussian likelihood this is exactly the covariance of the responses:</p> <pre><code>grid = jnp.linspace(0, 1, 50)\nSigma = model.predict_cov(result.params, grid)     # (50, 25, 25)\n</code></pre> <p>You can also predict the mean response with <code>model.predict_mean(result.params, grid)</code>.</p>"},{"location":"quickstart/#5-plot-covariance-ellipses-in-pca-space","title":"5. Plot covariance ellipses in PCA space","text":"<p>A covariance matrix over 25 neurons is hard to view directly. A standard visualisation projects each condition's covariance into a shared 2-D PCA space and draws it as an ellipse \u2014 the ellipse's axes are the eigenvectors of the projected covariance, scaled by the square roots of its eigenvalues. Compute a PCA basis once (e.g. from the neurons' grand covariance), project every <code>Sigma[c]</code> into it, and draw one ellipse per condition, coloured by the condition value, to see the covariance rotate and stretch smoothly across the periodic axis:</p> <pre><code>import numpy as np\nimport matplotlib.pyplot as plt\nfrom matplotlib.patches import Ellipse\n\n# A shared 2-D projection: top-2 principal axes of the grand covariance.\nSigma = np.asarray(Sigma)\ngrand = Sigma.mean(axis=0)                          # (25, 25)\nevals, evecs = np.linalg.eigh(grand)\nW = evecs[:, -2:]                                   # (25, 2) projection\n\nfig, ax = plt.subplots()\ncmap = plt.get_cmap(\"twilight\")                     # periodic colormap\nfor c, xc in enumerate(np.asarray(grid)):\n    cov2 = W.T @ Sigma[c] @ W                        # (2, 2) projected covariance\n    vals, vecs = np.linalg.eigh(cov2)\n    angle = np.degrees(np.arctan2(vecs[1, -1], vecs[0, -1]))\n    width, height = 2.0 * np.sqrt(np.maximum(vals, 0.0))\n    ax.add_patch(Ellipse((0, 0), width, height, angle=angle,\n                         fill=False, edgecolor=cmap(xc), lw=1.2))\nax.set_aspect(\"equal\")\nax.autoscale()\nax.set(xlabel=\"PC 1\", ylabel=\"PC 2\",\n       title=\"Condition-dependent covariance (PCA space)\")\nplt.show()\n</code></pre> <p>The <code>[viz]</code> extra (<code>pip install \"wishart-process-em[viz]\"</code>) pulls in <code>matplotlib</code> and <code>scikit-learn</code> for exactly this kind of plotting.</p>"},{"location":"quickstart/#6-spike-counts-the-poisson-variant","title":"6. Spike counts: the Poisson variant","text":"<p>For raw spike counts, switch the likelihood to <code>\"poisson\"</code> (or <code>\"negative_binomial\"</code>). The latent no longer integrates out analytically, so <code>fit</code> builds a QMC lattice and estimates the marginal likelihood by quasi-Monte-Carlo at every step \u2014 a stochastic-EM-style optimisation:</p> <pre><code>counts_model = WishartProcessModel(basis, num_neurons=25, rank=3,\n                                   likelihood=\"poisson\")\ncounts, _ = counts_model.sample(jxr.PRNGKey(2), counts_model.init_params(jxr.PRNGKey(3)), X)\n\nresult = fit(counts_model, counts, X, num_steps=1000)     # uses QMC each step\nSigma_latent = counts_model.predict_cov(result.params, grid)   # covariance of eta\n</code></pre> <p>For count models, <code>predict_cov</code> returns the covariance of the linear predictor \\(\\eta\\), not of the counts themselves. For the covariance of the observed counts (which also reflects the softplus link and the count noise), use a Monte-Carlo estimate:</p> <pre><code>obs_cov = counts_model.predict_observed_cov(jxr.PRNGKey(4), result.params, grid,\n                                            num_samples=500)   # (50, 25, 25)\n</code></pre> <p>Identifiability of Poisson noise covariance</p> <p>With a plain Poisson likelihood the count variance is tied to the mean (\\(\\operatorname{Var} = \\mathbb E[y]\\)), so there is little room for the latent Wishart covariance to express itself: the shared, condition-dependent covariance is only well-identified when the data are meaningfully over-dispersed (roughly \\(\\operatorname{Var}/\\operatorname{mean} \\gtrsim 1.5\\)). If your counts are close to Poisson, prefer the <code>\"negative_binomial\"</code> likelihood \u2014 its per-neuron dispersion absorbs the single-neuron over-dispersion and lets the Wishart process capture the genuine shared covariance structure. Always sanity-check the empirical Fano factor before interpreting a Poisson covariance fit.</p>"},{"location":"quickstart/#where-to-go-next","title":"Where to go next","text":"<ul> <li>Model \u2014 the math behind the basis, covariance family, and   estimators.</li> <li>Command line \u2014 run fits, evaluations, and sampling from the shell.</li> <li>API reference \u2014 every public function and class.</li> </ul>"}]}